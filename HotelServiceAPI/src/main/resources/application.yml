server:
  port: 8082

spring:
  application:
    name: HOTEL-SERVICE
  config:
    import: optional:configserver:http://cloud-config-server-docker:8070/
  batch:
    jdbc:
      initialize-schema: ALWAYS
    job:
      enabled: false





